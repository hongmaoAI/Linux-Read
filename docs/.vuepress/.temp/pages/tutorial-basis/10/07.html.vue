<template><div><h1 id="重点回顾" tabindex="-1"><a class="header-anchor" href="#重点回顾"><span>重点回顾</span></a></h1>
<ul>
<li>
<p>由于核心在内存中是受保护的区块，因此我们必须通过 shell 将我们输入的指令与 Kernel 沟通，好让 kernel 可以控制硬件来正确无误的工作</p>
</li>
<li>
<p>学习 shell 的原因主要有：</p>
<ul>
<li>
<p>文字接口的 shell 在各大 distribution 都一样</p>
</li>
<li>
<p>远程管理时，文字接口速度较快</p>
</li>
<li>
<p>shell 是管理 linux 系统非常重要的一环，因为 linux 内很多控制都是以 shell 撰写的</p>
</li>
</ul>
</li>
<li>
<p>系统合法的 shell 均写在 /etc/shells 文件中</p>
</li>
<li>
<p>用户默认登录取得的 shell 记录于 /etc/passwd 的最后一个字段</p>
</li>
<li>
<p>bash 的功能主要有：</p>
<ul>
<li>命令编修能力</li>
<li>命令与文件补全功能</li>
<li>命令别名设置功能</li>
<li>工作控制、前景背景控制</li>
<li>程序化脚本</li>
<li>通配符</li>
<li></li>
</ul>
</li>
<li>
<p>type 可以用来找到执行指令为何中类型，也可以用于与 which 相同的功能</p>
</li>
<li>
<p>变量就是以一组文字或符号等，来取代一些设置或是一串保留的数据</p>
</li>
<li>
<p>变量主要有环境变量与自定义变量，或称为全局变量与局部变量</p>
</li>
<li>
<p>使用 env 与 export 可以观察环境变量，其中 export 可以将自定义变量转成环境变量</p>
</li>
<li>
<p>set 可以观察目前 bash 环境下的所有变量</p>
</li>
<li>
<p><code v-pre>$?</code>为变量，是前一个指令执行完毕后的回传值。在 linux 回传值为 0 表示执行成功</p>
</li>
<li>
<p>locale 可用于观察语系资料</p>
</li>
<li>
<p>可用 read 让用户由键盘输入变量的值</p>
</li>
<li>
<p>ulimit 可用以限制用户使用系统的资源情况</p>
</li>
<li>
<p>bash 的配置文件主要分为 login shell 与 non-login shell。</p>
<ul>
<li>login shell 主要读取 /etc/profile 与 <code v-pre>~/.bash_profile</code></li>
<li>non-login shell 则仅读取 <code v-pre>~/.bashrc</code></li>
</ul>
</li>
<li>
<p>在使用 vim 时，若不小心按了 ctrl + s 则画面会被冻结。可以使用 ctrl + q 来解除冻结</p>
</li>
<li>
<p>通配符主要有：<code v-pre>*、？、[]</code> 等</p>
</li>
<li>
<p>数据流重导向通过 <code v-pre>&gt;、2&gt;、&lt;</code> 之类的符号将输出的信息转到其他文件或装置去</p>
</li>
<li>
<p>连续命令的下达可通过 <code v-pre>;、&amp;&amp;、||</code> 等符号来处理</p>
</li>
<li>
<p>管线命令的重点是：</p>
<ul>
<li>管线命令仅会处理 standard output，对于 standard error output 会忽略</li>
<li>管线命令必须要能够接受来自前一个指令的数据成为 standard input 继续处理才行</li>
</ul>
</li>
<li>
<p>本章介绍的管线命令主要有：cut、grep、sort、wc、uniq、tee、tr、col、join、paste、expand、split、xargs 等</p>
</li>
</ul>
</div></template>


